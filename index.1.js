const traceBrowser = require('./dist/trace-client-browser');
const traceNode = require('./dist/trace-client-node');
const server = require('./dist/server');

module.exports = {
  //...? together or separate sibling files for each?
}